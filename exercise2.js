const lettersArray = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'];

// modulus % will divide the numbers and show the remaining amount
if (lettersArray.length % 2 === 0) {
    console.log('even');
} else {
    console.log('odd');
}